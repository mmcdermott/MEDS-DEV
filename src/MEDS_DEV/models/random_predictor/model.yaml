commands:
  unsupervised: null
  supervised:
    hyperparameter_tuning: null
    training: null
    prediction: >-
      python -m MEDS_DEV.models.random_predictor.generate_random_predictions
        labels_dir={labels_dir}
        predictions_fp={predictions_fp}
